/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      // ...other domains,
      'useful-birthday-d8d2f80a21.media.strapiapp.com'
    ]
  }
};

export default nextConfig;
